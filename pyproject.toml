[project]
name = "ontology-alignment-curation"
version = "0.1.0"
description = "Streamlit workflow for ontology alignment curation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "pandas>=2.2.0",
  "streamlit>=1.42.0",
]

[tool.uv]
package = false
